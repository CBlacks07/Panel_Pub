{
  "name": "panel-pub-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace=apps/web",
    "mobile": "npm run start --workspace=apps/mobile",
    "build:web": "npm run build --workspace=apps/web"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "overrides": {}
}
